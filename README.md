# Quantium starter repo
This repo contains everything you need to get started on the program! Good luck!
1. setup
pip install pandas dash
pip install python "dash[testing]"
2. convert all three cvs into one under the product name,sales and region columns only